import urllib
import logging
import json

#def r_run_cmd(line, config):
#    logger = logging.getLogger('bot.cmd.r')
#    return 'PRIVMSG ' + twatbot + ' :.btc\r\n'
#    if url.getcode() != 200:
#        logger.error('Request failed with http error: ' + str(url.getcode()))
#        return False
#    data_parse = json.loads(url.read())
#    logger.info('Completed command')
#    return 'PRIVMSG ' + config['channel'] + ' :' + '\x0304[GDAX/LTC]\x03 > Last Price: ' + str(data_parse['price']) + ' | Volume: ' + str(data_parse['volume'])


